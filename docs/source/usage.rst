Usage Guide
===========

Building the Project
--------------------

Use Visual Studio Code:

* Press ``Ctrl+Shift+B`` to build
* Press ``F5`` to debug

Or use command line::

    cmake --build build

Running Tests
-------------

To run tests::

    cmake --build build --target test 